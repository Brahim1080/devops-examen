// TODO: Add here the function to validate

const isEmpty = (label) => !label || label.length === 0;
function isValidGamerTag(){
    return false;
}
exports.isEmpty = isEmpty;
exports.isValidGamerTag = isValidGamerTag;
